# VuelaClaim — CI en Codeberg (Woodpecker), lane alternativo sin cuota de GitLab
steps:
  test:
    image: node:22-alpine
    commands:
      - node --test test/ec261.test.js test/more.test.js test/appr.test.js test/watcher.test.js test/fuzz.test.js test/server.test.js
