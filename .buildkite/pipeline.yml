# VuelaClaim — CI alternativo en Buildkite (lane sin cuota propia de GitLab)
steps:
  - label: ":node: tests"
    command: "node --test test/ec261.test.js test/more.test.js test/appr.test.js test/watcher.test.js test/fuzz.test.js test/server.test.js"
    agents:
      queue: "default"
